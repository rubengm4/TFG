# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Virtual envs / local tooling
venv/
env/
.algenv/
.idea/
.vscode/

# Dependencies / artifacts (large)
node_modules/

# Secrets / env (runtime uses Compose env_file / bind mount)
.env
**/.env

# Git / docs / OS noise
.git/
.gitignore
README.md
docs/
.DS_Store
*.log
*.pot
*.swp
*.swo
*~

# Heavy local dirs (still available at runtime via `.:/app` bind mount)
media/
Algoritmos/

# Algorithm seed ZIPs (large). Excluded from build context; available at runtime via bind mount `.:/app`.
algovision/analysis/seeds/bundled_algorithms/*.zip
